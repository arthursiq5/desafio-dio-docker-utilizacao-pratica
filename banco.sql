CREATE TABLE dados (
    id INT NOT NULL AUTO_INCREMENT,
    nome VARCHAR(50),
    sobrenome VARCHAR(50),
    endereco VARCHAR(150),
    cidade VARCHAR(50),
    host VARCHAR(50),
    primary key(id)
);
